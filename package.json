{
  "name": "shift-management-app",
  "version": "1.0.0",
  "description": "Shift scheduling, attendance tracking, and payroll management app",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm install --prefix frontend && npm run build --workspace=frontend",
    "build:all": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
